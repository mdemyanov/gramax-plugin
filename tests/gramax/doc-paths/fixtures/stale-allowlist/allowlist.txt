# Фикстура для FR-027. Три сценария рассинхрона на трёх строках content/sample.md:
#   строка 3 — путь исчез вовсе, паттерна на строке больше нет.
#   строка 5 — легитимная историческая запись: ожидаемый путь всё ещё на месте.
#   строка 7 — запись держит СТАРЫЙ путь (docs/qa-reports), а строку переписали: там
#     теперь другой, реально нерабочий указатель (docs/adr/…), совпадающий с тем же
#     паттерном. Проверка Important review: freshness обязана поймать подмену пути,
#     а не удовлетвориться тем, что какой-то docs/-путь на строке всё ещё есть.
# Гейт обязан назвать 3 и 7, не тронуть 5.
content/sample.md:3 — docs/qa-reports — заведомо протухшая запись (проверка FR-027, путь исчез целиком)
content/sample.md:5 — docs/qa-reports — легитимная историческая запись, процитированная команда
content/sample.md:7 — docs/qa-reports — запись держит прежний путь; строку переписали, реальный указатель теперь другой (проверка Important review, подмена пути)
